# Preview Build

## Purpose

Build a per-PR preview.

## Instructions

Use the `preview-build` implementation for the execution details.
The capability owns the public action name and the reason this action exists; the implementation owns the method.