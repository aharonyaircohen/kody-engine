# Release Publish

## Purpose

Tag, publish to npm, open GitHub release.

## Instructions

Use the `release-publish` implementation for the execution details.
The capability owns the public action name and the reason this action exists; the implementation owns the method.