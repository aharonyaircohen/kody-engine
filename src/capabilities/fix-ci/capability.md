# Fix CI

## Purpose

Fix the latest failing CI run on a PR.

## Instructions

Use the `fix-ci` implementation for the execution details.
The capability owns the public action name and the reason this action exists; the implementation owns the method.