# Resolve

## Purpose

Merge base into a PR and resolve conflicts.

## Instructions

Use the `resolve` implementation for the execution details.
The capability owns the public action name and the reason this action exists; the implementation owns the method.