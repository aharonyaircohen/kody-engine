# Release Prepare

## Purpose

Bump version, update changelog, open release PR.

## Instructions

Use the `release-prepare` implementation for the execution details.
The capability owns the public action name and the reason this action exists; the implementation owns the method.