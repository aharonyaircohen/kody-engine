# Sync

## Purpose

Update a PR branch from base without an agent run.

## Instructions

Use the `sync` implementation for the execution details.
The capability owns the public action name and the reason this action exists; the implementation owns the method.